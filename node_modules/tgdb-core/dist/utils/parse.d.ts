export type Table = Record<string, number>;
interface Header {
    page_id: string;
    page_index: number;
    next_msg_id: number | null;
    prev_msg_id: number | null;
}
interface Index {
    header: Header;
    table: Table;
}
interface Data {
    header: Header;
    dbName: string;
    dbMsgId: number;
    value: string;
}
export declare function parseIndex(str: string): Index;
export declare function parseTable(tables: string[]): Table;
export declare function parseData(str: string): Data;
export {};
